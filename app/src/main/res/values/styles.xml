<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Primary app theme: Material 3 dark, Ruby accent -->
    <style name="Theme.RubyFPV" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/ruby</item>
        <item name="colorOnPrimary">@color/ruby_on</item>
        <item name="colorPrimaryContainer">@color/ruby_dark</item>
        <item name="colorOnPrimaryContainer">@color/ruby_on</item>
        <item name="colorSecondary">@color/ruby</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorSurfaceContainer">@color/surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>
        <item name="colorOutline">@color/outline</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:navigationBarColor">@color/bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.RubyFPV.Fullscreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Live USB viewer keeps its own minimal fullscreen look -->
    <style name="Theme.Live" parent="android:Theme.NoTitleBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

</resources>
